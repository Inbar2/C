#include "Executor.h"
#include "zlog4c.h"

int main()
{


CreateExecutor();

DestroyExecutor(exe);

AddTask(exe, taskFunc, period, context);

StartExecutor(_exe);

IsExecutorRunning();


return 0;
}

